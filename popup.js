// popup.js — minimal script for the shortcuts-only popup
document.addEventListener('DOMContentLoaded', () => {
  // nothing dynamic required for the minimal UI
  // Keep the file so the popup can load scripts without errors in CSP environments
});